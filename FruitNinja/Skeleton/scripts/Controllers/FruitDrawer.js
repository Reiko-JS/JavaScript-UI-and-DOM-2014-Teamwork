define(function(require) {
    var _context = null;

    // Constructor
    function FruitDrawer(context) {
        _context = context;
    }

    FruitDrawer.prototype.drawGameBlocks = function(fruits) {
        for (var i = 0; i < fruits.length; i++) {
            // Draw
        }
    };

    return FruitDrawer;
});